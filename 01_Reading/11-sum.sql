select sum(age) from users;

/*La función SUM() de SQL
La SUM()función devuelve la suma total de una columna numérica.*/

-- Devuelve la suma de todos Quantitylos campos de la OrderDetails tabla:

SELECT SUM(Quantity)
FROM OrderDetails;