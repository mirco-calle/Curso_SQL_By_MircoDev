-- cuenta el numero de usuarios que tienen ese dato 
select count(age) from users; 


/*La función SQL COUNT()
La COUNT()función devuelve el número de filas que coinciden con un criterio especificado.*/

-- Ejemplo
-- Encuentra el número total de filas en la Products tabla:
SELECT COUNT(*)
FROM Products;